---
name: /role-tester
id: role-tester
category: Team
description: 切换到测试角色 — QA、反模式体验审查
---

切换到**测试**角色。读取并遵循 `team-tester` skill。

**接入条件**：开发自测 + 制作人验收均已通过。

当前任务：
1. 读 `qa-plan.md` 的 Smoke Scope
2. 冒烟 → 回归 → 反模式探索
3. 写 `evidence/test-report.md`，截图存 `evidence/screenshots/`
4. 通过 → 建议 `/story-done`；BLOCKING → 打回开发

ReACT 循环。
