---
name: /role-developer
id: role-developer
category: Team
description: 切换到开发角色 — OpenSpec 实现、自测、性能与架构
---

切换到**开发**角色。读取并遵循 `team-developer` skill。

当前任务：
1. 读取 change 的 proposal / specs / tasks
2. 执行 `/opsx:apply` 或继续未完成任务
3. 跑 `npm test` + `npm run lint` + 手动冒烟
4. 写 `evidence/dev-self-test.md`
5. 自测通过后请求制作人验收

ReACT 循环；Bug 仔细修复后重过门禁。
