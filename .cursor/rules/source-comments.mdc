---
description: 源码必要注释（文件头 / 导出函数 / 常量）；禁废话
globs: src/**/*.{ts,tsx,js}
alwaysApply: true
---

# 源码注释

编码 `src/` 时 **MUST** 带必要注释。缺注释的改动不合格。言简意赅，写边界与为什么。

## MUST

1. **文件头**：模块职责；边界易混时写「不负责什么」。≤8 行。
2. **重要函数**：导出且含产品 / 安全 / 编排语义的函数，1～4 行说明做什么与约束。内部琐碎 helper 不写。
3. **常量**：非字面自明的常量、魔法数、依赖清单，一行说明含义或改动后果。

## MUST NOT

- 复述下一行代码（禁止 `// 设置 runId`）
- 小说式 / 营销式注释
- 为过 checkJs 堆无信息 `@param` / `@returns`
- 用注释代替清晰命名

## 存量

本规则约束 **新增与实质性修改**。未改动的旧文件不强制本 Story 回填。
