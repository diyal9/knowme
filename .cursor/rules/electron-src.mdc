---
description: Electron 源码约定 — src/ 主进程、预加载、IPC
globs: src/**/*
alwaysApply: false
---

# Electron 源码约定

## 架构

- **主进程** `src/main.js` 薄 boot → `src/main/index.ts` 创建 `ctx` 后 `create`（`boot` / `agent-runtime` / `icons` / `shell` / `knowledge` / `workbench` / `process-guards`）→ `ipc-deps.bindCoreIpc(ctx)` → `registerCoreIpc` 按域 `pick`（禁止 vm concat、`part-*`、`scope.ts`、`attach` 入口）
- **预加载** `preload.js`：`contextBridge` 暴露 `window.api`
- **渲染** React feature 包，禁止 `ipcRenderer`，禁止页面级 `src/*.html`（`attention-toast.html` 除外）

## 安全

- 渲染进程禁用 `nodeIntegration`
- 禁止在 preload 外直接使用 `require('fs')`
- 禁止 `eval()`、`debugger;`

## 数据

- 会话/设置/索引：`%APPDATA%\KnowMe\`
- 正文：用户绑定的本地文件夹或 GitLab
- 产品 OKF：`knowledge/` · 产品 Memory：`memory/`（均在用户数据目录）
- 设置 → **知识库与记忆** 可导入/导出 OKF 包
- 产品叙事保持知识工作台 / 工作伙伴，不写成桌面笔记应用

## 注释

改 `src/` 必须带必要注释（文件头 / 重要导出 / 非自明常量），见 `source-comments.mdc`。

## 改后自检

修改 `src/` 后运行 `npm test && npm run lint`。
