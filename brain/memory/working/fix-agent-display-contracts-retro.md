# Agent 显示契约修复回顾

反复出现的 ID 泄露源于前后端各自拼显示名称，测试只覆盖少数手工样例。装配和权限误显示则由列表投影丢字段、空值兜底表达错误事实造成。

本次把路由名称归一化放到导入/保存共用边界，用真实内置目录与真实导入验证，并将授权成功限定为明确探测证据。后续新增 Agent 应遵循 `src/catalog/README.md`，保留执行 ID 与展示名称分离，覆盖缺失、未知、错误和文本交付场景。

验证：完整 npm run check 通过。未进行真人或桌面逐页验收，不把自动化通过等同于所有视觉状态已验证。

- 后续核查：任务启动、模型生成、工具执行、文件交付必须区分；技能提示词不能代替实际工具。确认计划需要与执行和验收契约一致，禁止通过纯答复路径承诺代码文件交付。

## 用户确认才结束
协作执行完成不等于用户接受。将成果产出 readiness 与验收 completion 分开；统一 summary card，等待无超时，修订继续同任务。兼容旧自动完成时必须保留成果和版本，不重新执行。收尾合并最新 brief 避免覆盖执行中补充。
