# Retro: industry-tone-examples

## 做了什么

设置「我的记忆」增加结构化行业选择；缺飞书事实时，今日优先级空态用 catalog 确定性占位示例，并注入轻量个性化口吻。

## 学到什么

- 空态示例应确定性改写，不宜只依赖 prompt「禁止举例」（模型仍会编销售向例子）
- 行业字段应与「关于我」并存，不要用自由文本替代结构化 catalog

## 后续

- 写作入口可复用同一 `industry-profile` catalog
- 上线前建议手测：设置保存 → 今日优先级空事实路径
