# v0.4.0 重构 retro

日期：2026-08-18

## 结论

- v0.4.0 = React+TS Renderer **工程基线**，不是产品 1:1 完成
- 收口 change：`refactor-checkpoint-closeout`（验收语义升级为「v0.4.0 工程基线通过」）

## 本轮修复

1. **主规格 SSOT**：`agent-chat-ux` / `agent-run` 移除气泡「应用到文件」，与 `simplify-assistant-reply-chrome` 及实现一致
2. **ContentView**：`useCommittedBlocks` 增加 `bound` 守卫，source 切换首帧不展示旧 blocks
3. **LLM Endpoint**：`createIpv4FirstLookup()` 替代 `family:4`，IPv4 优先且允许 IPv6-only

## 验证数字（2026-08-18 实测）

- unit：1610 pass / 0 fail / 51 skip
- renderer：268 tests / 52 files
- `npm run check` + `renderer:build` 全绿

## 诚实缺口

- 薄表面清单在 `openspec/changes/BACKLOG.md`
- 未把检查点/门禁全绿冒充产品完成
- v0.5.0 增长项与重构欠账已分离（见 handoff）

## 教训

- 勿用 PowerShell `Set-Content -NoNewline` 写含中文的 TS 源文件
- 收口 change 保持单一活跃项，证据数字须本轮实测
