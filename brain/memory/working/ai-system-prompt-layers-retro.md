# Retro: ai-system-prompt-layers

## 做对了什么

- 把「可编辑 system」拆成固定底座 + 用户偏好，避免用户误删防幻觉规则
- 顺带修好多轮对话未进 API 的隐性缺陷
- 用指纹迁移兼容旧默认与自定义配置

## 下次注意

- 带 Key 的模型侧风格验收应进 Smoke（或标为人工 P1）
- openspec CLI 未装时需手动归档（与既有 change 一致）

## 可升格

- 模式：「产品固定指令 / 用户可填偏好」可复用到其它 AI 入口
