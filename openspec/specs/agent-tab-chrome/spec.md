# Spec: Agent 顶栏 Chrome

> 已被 `agent-session-tabs` 取代：主切换为多 Session Tab（New Agent | Tabs | + / 历史 / ⋯），不再以「通用 / 写作 / 编程」胶囊为主入口。详见 `openspec/specs/agent-session-tabs/spec.md`。

## 历史说明（归档保留）

- 原设计：左侧 Agent 类型分段 + 右侧 Session 工具群
- 现设计：每个打开的 Tab 即一个独立 Session / transcript
