# Spec: snippet-library

> **Status: REMOVED**（`rail-knowledge-module`，2026-07-21）

独立「片段库」产品面已下线。工作台不再提供片段库入口；主进程不再暴露 `list-snippets` / `create-snippet` / `update-snippet`。

磁盘已有 knowledge/ 技能概念数据保留。Agent/编辑器若通过 `listSkills` 支持 `/` 引用，属技能能力，非片段库 UI。
