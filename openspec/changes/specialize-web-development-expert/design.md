# 设计

保留 software-engineer 内部 ID，统一 catalog、capability.manifest.json、manifest.json、EXPERT.md 的名称、版本和技能依赖。HTML/CSS、Vue、React、TypeScript 与网站/Web 系统范围进入提示词、SOP、输入输出和路线标签。

frontend-design、imagegen-frontend-web 从用户指定本地源复制完整方法正文，只本地化包元数据；保留 SOURCE.md 和可用的 LICENSE.txt。两者属于必需安装依赖，运行时仍按路线只装配当前方法。网页视觉参考路线只承诺在工具可用时真实生图，普通开发不强制出图。

同步使用正式 hub.updateCapability 安装链路，先备份目标包和安装账本，阻止同一专家有活跃任务时同步；新建临时快照验证依赖，不覆盖历史会话快照。
