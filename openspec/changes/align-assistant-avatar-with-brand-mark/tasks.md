## 1. Align the in-app brand mark

- [ ] 1.1 Rebuild the assistant panel avatar SVG with the master icon node coordinates and connection topology.
- [ ] 1.2 Align the avatar carrier navy, corner rounding, and coral origin node to the brand palette, and drop the dead center-node styles.

## 2. Verification

- [ ] 2.1 Add a regression test that compares the panel mark coordinates against `assets/brand-src/knowme-icon.svg`.
- [ ] 2.2 Run `npm test` and `npm run lint`, then restart KnowMe and record development self-test evidence.
