# 验证计划

## Smoke Scope
- 遍历所有内置专家 manifest，核对模式数量、中文名称与 readiness 一致。
- 新建专家保存、加载及 Hub 映射，核对装配字段。
- 真实文件夹导入，核对旧模式名称补齐及保存后展示。
- 无效、重复路由拒绝；归一化幂等且执行字段不变。
- 组件级验证跨类型依赖名称和安装按钮、权限值、失败授权重试及交付摘要。
- 完整 npm run check。

真实桌面逐页验收、真实飞书授权和打包发行测试不在本次已执行证据中。
