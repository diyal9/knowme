# Agent 显示契约统一

## 问题
专家首屏泄露任务模式 ID，能力中心丢失装配字段，权限与授权状态及交付摘要出现误导。新建和导入 Agent 需要复用一致的展示规则。

## 范围
修复 2026-09-16 界面审查确认的七类问题。统一任务模式名称；完整保留任务入口；保留并解析跨类型装配依赖；按值显示声明权限；以明确证据显示授权成功；保留已确认交付约定；中文展示状态及风险。

## 兼容策略
保持执行 ID、工具、权限和业务路由含义。旧包无 label 时按共享规则补齐；导入包的空或重复路由 ID 校验失败。新建专家仍要求名称，并由统一声明及 Hub 投影保留技能和连接器。
