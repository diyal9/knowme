# 自动化体验验收

- 内置 26 条模式完整显示，名称不为内部 ID，首屏与 readiness 一致。
- 专家列表只含专家时，详情仍能显示代码审查和飞书，并提供可用的依赖安装按钮。
- 声明禁止联网及空工具权限时显示禁止、不允许调用。
- 授权检测错误时保持重试入口；明确成功后才显示已授权并关闭提示。
- 待验收、已接受文本交付仍显示已确认约定。

以上均为数据/组件自动化断言。未声称完成真实桌面逐页验收。

追加自动化验收：同一次计划确认只显示一个用户发言；历史确认仍可见，普通重复消息保留。
