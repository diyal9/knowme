# Tasks

## 0. 基线与边界

- [x] 记录 CSS 规模、字体、间距、圆角、阴影与 `!important` 基线。（spec: 可量化视觉治理）
- [x] 冻结导航、布局结构、React/状态/IPC 与业务功能边界。（spec: 结构和功能保持不变）
- [x] 建立视觉系统契约与完成标准。（spec: 统一视觉语法）

## 1. 治理基础

- [x] 补齐 4–48px 间距、密度、圆角、阴影与控制尺寸令牌。（spec: 统一视觉语法）
- [x] 新增 `visual:audit` 与 `visual:check`，锁定非回退预算并接入 lint。（spec: 可量化视觉治理）
- [x] 新增视觉令牌与预算契约测试。（spec: 可量化视觉治理）

## 2. 能力中心样板

- [x] 按参考图收敛卡片字号、行高、内边距与信息密度。（spec: 能力卡片保持结构并改善层级）
- [x] 将样板中的常用字体、间距、控制尺寸改为共享令牌。（spec: 统一视觉语法）
- [x] 在真实 Electron 中补齐宽屏、850px 与窄窗截图矩阵。（spec: 能力卡片保持结构并改善层级）

## 3. 按体验域迁移

- [ ] 迁移专家协作主旅程并下调预算。（spec: 逐域非回退迁移）
- [ ] 迁移工作流、管线与审阅主旅程并下调预算。（spec: 逐域非回退迁移）
- [ ] 迁移设置、知识库、文件与辅助入口并下调预算。（spec: 逐域非回退迁移）

## 4. 层叠清理与最终验收

- [ ] 仅在视觉/行为等价后拆分超大 CSS 责任域。（spec: 结构和功能保持不变）
- [ ] 将 `!important` 收敛到不超过 30 处并逐项记录例外。（spec: 可量化视觉治理）
- [ ] 达成最终量化目标，完成全矩阵视觉、键盘与功能回归。（spec: 逐域非回退迁移）
