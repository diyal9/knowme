# 代码审查

## 范围

- 新增只读视觉熵审计、冻结预算、视觉治理契约与 OpenSpec。
- `tokens.css` 只新增间距、密度、控制尺寸、圆角和阴影令牌；旧阴影名保留兼容别名。
- `capability-hub.css` 只调整最终卡片覆盖层，未修改 React、DOM、事件、路由、状态、IPC 或业务数据。
- `package.json` 只增加审计命令并把非回退检查接入 lint。

## 影响分析

GitNexus 索引为最新。`CapabilityHubSurface` upstream impact 为 LOW、直接影响 0；本批未修改该符号。CSS 选择器与新增审计脚本尚无图索引，已按源码引用和定向契约测试补查。审计脚本仅由 npm 命令和视觉契约测试调用。

## 审查结论

- 未发现功能或结构变化。
- 新共享阴影通过 alias 保持旧 computed value；新增令牌不引入运行时依赖。
- 预算先锁当前实测 ceiling，后续迁移只能下调，防止以“大重构”为名继续增加视觉债务。
- 能力卡片的变化限制在字号、行高、间距和图标尺度，属于已批准的局部视觉优化。

## 已知风险

- 当前工作区有大量既有未提交变更，全仓 `detect_changes` 会包含非本 change 范围，不能单独作为归因依据。
- 声明级审计比历史行级抽样更严格，指标绝对值不可直接互换；已在 evidence 中记录口径。
- 全页面截图矩阵和剩余体验域迁移仍未完成，本 change 暂不归档。
