# OpenSpec BACKLOG（重构检查点收口 · 2026-08-18）

**活跃 change = 0**（`openspec list`、`openspec-health.js`、`git ls-files openspec/changes` 去掉 `archive/` 均为空）。

下面是**未交付**需求，下一轮用 `/opsx:propose` 单独立项。不要把归档当成已做完，也不要把本检查点再留成活跃 change。

已从 `archive/` 删除：文案/导航 rename 类 change，以及 2026-07 便签时代归档（代码与 `openspec/specs/` 仍在）。
