# Acceptance

- 规划回复含未决问题时，界面不提供确认入口。
- 即使渲染层状态异常，主进程也不会为混合规划签发确认凭证。
- 确认凭证与任务、专家和规范化计划绑定，十分钟过期且只能使用一次。
- Web 页面生成任务自动要求 `write_file` 成功回执和文件 artifact，不能以聊天正文完成。
- Web 开发专家 3.1.0 已同步当前 KnowMe 运行时；写入仍经过项目范围检查和草稿审批。

