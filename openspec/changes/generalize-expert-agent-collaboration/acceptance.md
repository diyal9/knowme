# Producer Acceptance

结论（2026-09-05）：真实任务已完成初次生图、对话修改、原图引用编辑、大图查看、接受成果及重启恢复。不是所有专家专业能力或所有图像模型都已验收通过。详见 `evidence/live-e2e-2026-09-05.md`。下方旧记录仅证明组件和模拟工具场景通过。

## 本次真实复测发现

- 原任务 `task-mto99tkr-2c3kl` 的生图调用成功并保存了图片，但声明校验仅按工具名称匹配“保存”，误报失败。
- 新建实测任务 `task-mtoadfga-fnupq` 首轮再次暴露工具中转层丢失 receipt 的问题，不能计为通过。
- 修复中转后，修改轮 `expert_task-mtoadfga-fnupq_mtoalpsk` 真实生成并通过校验，大图完整可见；但用户要求 1 张却调用生成 3 次，仍不满足数量和交互验收。
- 发现历史恢复把文件找回直接标为 verified，以及执行历史使用聊天恢复函数丢弃工具记录。必须保留原判定，修复后再验证完整对话与重进恢复。
- 真实实测证据与模拟测试必须分别标注；未经当前流程实测，不将 22 个专家的专业任务能力宣称为全部通过。

## 历史模拟验收记录（不代表真实供应商通过）

- 平台根据 Capability Manifest 的工具、连接器、交付类型、证据和完成条件编排，不再按专家 ID 决定正常执行逻辑。
- 22 个内置专家均具备显式 Manifest v3、完整专业 SOP，并通过相同的正常交付、验收与异常阻断矩阵。
- 文本答复默认留在对话；只有 Agent 声明或用户明确要求文件时才形成文件成果。
- 图片成果显示真实缩略图，图片卡片只承担预览；状态、验收和修改保持在对话交互区。
- 退回修改复用底部唯一输入框；多成果任务要求先选择目标，单成果任务直接提交修改意见。
- 任务目标、编号、消息、所有 Artifact 引用和执行证据可跨重启恢复，失败不生成虚假成果。

## 运行态视觉验收

- 视口：1600 × 1000，无头 Chromium，正式 Vite 渲染器 + 隔离 API seam。
- 图片自然尺寸：1600 × 1000；缩略图与大图均为 `object-fit: contain`。
- 页面只有 1 个主输入框，退回修改区域没有额外输入框。
- 图片预览 1 个，点击后大图弹窗完整可见；控制台错误 0。
- 证据：`evidence/expert-room-runtime.png`、`evidence/expert-room-runtime-dialog.png`、`evidence/expert-room-runtime.json`。

## 验收边界

本次验证覆盖 KnowMe 通用编排与内置 Agent 包契约。真实第三方模型或图像供应商的可用性、额度与输出审美受外部账号和服务状态影响，不由平台自动化门禁伪造为成功。
