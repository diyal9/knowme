# QA-BI-01：真实执行把失败提示当成交付

## 环境与边界

- 独立 Electron profile：`D:/USERCA~1/Temp/knowme-expert-qualification-e3808573825e42908aef0a0f2d0f20de`，实测通过 Electron `app.getPath('userData')` 核对。主用户任务未写入或重试。
- `KNOWME_TEST_SEAM=1` 仅用于指定测试资料目录；未设置输出 fixture，未传 fakeApply/dryRun/skipAudit，没有替换 window.api、LLM 或 tool ports。
- 从用户配置复制模型设置、加密密钥和必要 capability 包到隔离目录，不复制用户对话/任务/自动化/知识库。初次缺 Chromium Local State 导致密钥不可解密；仅返回配置阻塞且没有产物。关闭隔离进程、备份该目录的 Local State 并复制对应源 Local State 后，安全存储解密检查返回 true。未打印明文密钥。
- 实际模型：`qwen3.8-flash`（界面 Qwen 3.8 Flash）。测试输入为 evals.json 第一题虚构渠道数据，不是用户业务数据。
- 隔离目录手工暂存商业洞察专家2.1.0及三个源 Skill（报告1.1.0），而非通过产品升级器安装。因此本次不验收升级器或已安装包迁移。
- 使用真实 preload `expertTaskCreateStart`、`expertTaskRetry` 启动，再从工作台点击任务检查。尚未覆盖 UI 澄清/确认流程。

## 实际结果（修复前）

任务 `task-mtolrzt1-npiq8`，运行 `expert_task-mtolrzt1-npiq8_mtoly2a0`。2026-09-05T16:39:07Z 开始执行；约2秒后进入 `review`。

模型调用 `business_metrics_analysis`。实际工具记录：

```json
{
  "name": "business_metrics_analysis",
  "status": "fail",
  "digest": "未注册工具: business_metrics_analysis"
}
```

平台却给出 `gateStatus=verified`、`verificationPassed=true`，创建 `primary` 的 answer v1，待验收；正文为：

> 工具调用未成功：请求参数或工具能力不匹配。
> 请明确目标对象与参数后重试（例如补充文档 token、查询关键词）。

[界面证据](bi01-failure-as-delivery.png)：顶部“等待你验收”，对话只有失败提示，没有经营判断。截图在任务页面内容实际显示后保存；不以挂载中的空白截图充数。

结论：功能轨道 A2/A3 失败；专业任务没有生成可评价分析，B 未验证。不能用 Skill 对照的15/15或全量测试通过覆盖该失败。

## 已定位机制

1. Skill 的 L0 摘要只给名称/描述，模型将其转成同名函数；真正的读取接口是 `load_skill(skill_id)`。
2. `agent-recovery` 有 unknown_tool 说明却未将该类别加入有界纠正集合。
3. 模型循环将工具失败兜底写入正常答复候选；无必需工具/文件的 answer 契约随后通过，expert runtime 把该提示当成正式交付。

修复应在通用能力说明、恢复策略和结构化执行结果中完成，不添加商业洞察 ID 分支，不把所有历史失败都判成最终失败。修复后须重跑同题及真正的修改轮；本段保留为失败基线。

## 修复后真实复测：流程推进，不代表专业达标

重启且再次核对同一隔离 profile 后创建 `task-mtonnbn1-6i2qa`（QA-BI-01R），没有修改用户正式任务数据。实际模型仍为 Qwen 3.8 Flash。

- 首轮 `expert_task-mtonnbn1-6i2qa_mtonnbtg`，2026-09-05T17:26:45Z 至17:27:04Z：真实 answer v1、review。调用 search_knowledge 和 fabric_search，均返回“未找到相关知识条目”；本轮没有 unknown_tool，没有 load_skill 回执。不能将其称为真实触发并验证了错误恢复预算；该分支由定向测试覆盖。
- [首轮完整输出](bi01-live-answer-1.md)按实际会话 artifact 原文保存，未润色。总体 9.2%→3.9% 等主指标正确，但有硬性质量失败：虚构新版仅用于展示渠道、搜索下降早于/独立于改版、900访客为库存上限；表格把900写成810。拒绝扩预算的部分理由建立在编造事实上。
- 从真实页面唯一底部 textarea 输入纠正（展示27改9，明确要求撤回三项无依据事实），点击“发送”；未调用 review API 代替用户操作，未新增第二个输入框。
- 修改轮 `expert_task-mtonnbn1-6i2qa_mtonr46o`，17:29:42Z 至17:30:03Z：answer v2、review，previousVersionId 指向v1，原v1正文保留；评论完整保存在交付记录。新轮没有工具调用。
- [第二轮完整输出](bi01-live-answer-2.md)：正确重算21注册、2.1%、-7.1pp，算术拆分92→28→21，并撤回三项无依据事实。但又新增“维持上周搜索量级，89/1000=8.9%即可补回缺口”的不成立计算（题目没有其分母/89的推导，且维持上周量级应为900）；称未随机分配的新旧版流量“即刻就是天然对照组”；仍把落地页部署未知的展示率变化当作支持新版不利的依据；50%灰度和一周回滚建议缺少样本量/停止准则。篇幅也比首轮更长，未满足简洁要求。
- [修改后界面](bi01-revision.png)已人工查看：唯一底部输入框、正常正文、对话内验收动作；同时发现任务标题被用作一处发言者名称，旧“修改意见已送达”提示仍保留，尚非完整视觉验收通过。

功能判断：本例首轮真实分析输出及单输入框修改/版本保留通过；不覆盖24专家，不覆盖生图服务。专业判断：v1、v2均触及事实/计算红线，不合格；不能用“verificationPassed=true”表示专业质量已验收。绑定三个Skill只证明配置，不证明专业方法正文实际加载并执行；本例工具回执没有load_skill，需继续查上下文装配/选择与方法执行的证据，不直接断言Skill正文已注入。

补充：旧失败基线 task-mtolrzt1-npiq8 被错误置为review后，直接retry返回“当前任务不需要重新执行”；本补丁不会自动迁移历史伪成果。恢复旧任务需另行设计可追溯纠正，不删除历史或伪造新结果。

刷新渲染页后从工作台重新打开QA-BI-01R，正文仍包含v1、用户修改意见与v2，textarea数量1且未禁用。因此本例的对话恢复/输入入口通过。此前截图中可见的对话验收区在重开后的正文末尾不再出现，需作为恢复视图的一致性问题继续定位，不能将此判为完整重开体验通过。测试结束仅关闭隔离Electron进程，保留隔离资料供追查；未关闭或操作用户原应用。

## 当前门禁

- RQA06定向：164通过、13既有跳过、0失败；L0说明相关定向38通过。
- 本轮所有源码修改后的 `npm run check` 已exit0（执行session94238）：后端test、lint、80个renderer测试文件/520测试、typecheck全部通过；后台输出截断，本文不另报未保留下来的后端汇总数。
- GitNexus全工作区分析为CRITICAL：289文件、527符号、160受影响流程，包含长期共享改动，不能归因于本轮补丁或称其全部已审查。未commit。专业红线失败不因工程门禁通过而关闭。
