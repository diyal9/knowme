# RQA28：修订上下文接线改进，真实专业复验仍失败

2026-09-06。本轮目标是验证专家能否真正落实用户的修改意见，而不是仅换版本号。全部24专家的生产资格目标仍为ACTIVE；本轮没有新增合格专家。

## 工程改动与边界

- `src/lib/expert-task-runtime.ts` 的 `buildPrompt`：旧成果通过JSON.stringify封装为previous_deliverable/reference_only，完整已保存反馈放在旧稿之后；明确在平台/权限约束内，最新反馈覆盖旧目标、计划、旧稿的冲突部分，并同步正文、表格、参数及结尾。
- `src/lib/agent-run-executor/phases-model-tool.ts` 的 `finalizeResponse`：保留原收尾职责，追加保留最新用户更正的通用要求，包括长度恢复与grounding修复。未改变工具权限、预算公式、恢复次数和完成门禁。
- 新增 `tests/rqa28-revision-context.test.js` 三个测试。先在旧行为上3红，候选后与incomplete/runtime相关测试48/48绿；模型端为离线fixture，不证明模型能力。
- 原1000字反馈归一化、极紧预算下非锚点收尾提示可压缩、修订语义没有硬门禁等限制未修。本例反馈215字，不能归因于1000字边界。JSON是提示层数据定界，不是安全屏障。

GitNexus已在编辑前分析：buildPrompt LOW（直接execute），finalizeResponse HIGH（直接runGroundAndPersist/runModelToolLoop、共同run链路），并在改动前告知用户。execute的MEDIUM分析仅用于备选调查，本轮未修改execute。FTS与process资源读取不可用时，按当前源码补查，不假称完整图谱验证。独立复审通过内存还原验证两文件仅上述装配差异；见 `rqa28-feedback-runtime-review.md`。

## 原任务真实F01：新提示已送达，核心修改仍全部未落实

原任务 `task-mtpoekri-9ket3`，从原失败v2经唯一主输入框再次提交冻结的215字反馈，生成v3。run `expert_task-mtpoekri-9ket3_mtppy8ab`。这是同题再次修订，不是留出题、随机A/B或Skill单变量实验；历史多了一轮失败稿。

自有隔离QA在无运行任务时重启，加载候选源码；18个已安装专家包文件重启前后SHA256不变，没有安装或修改专家包。此18文件控制不冒充全部依赖/所有进程模块指纹。实际两次qwen3.8-flash请求进一步确认：第9条消息含新旧稿JSON、其后完整反馈和冲突优先级；第二请求第10条含新增最新更正要求及长度恢复提示。

结果为review，完整v3正文4773字。独立全文评分 **3/6、B4=0、违反反馈红线**：

| 核心反馈 | 实际v3 |
|---|---|
| 900×1200是请求值，另核实实返 | 仍以最终画布为目标并默认加工到精确尺寸 |
| 不默认裁切、拉伸、重采样或付费重试 | 仍有按需缩放/裁切、必要时高质量重采样 |
| 不合格先暂停、说明差异并征求授权 | 仍有命中则重生成、任一不合格即重生成或局部修复 |

v2/v3均43行，唯一差异是标题v2→v3。未改项、完整英文正负Prompt保留；本F01无工具、文件、图片、外发或付费操作记录。失败的是专业修订文本，不是已证实发生了越权付费。

实际刷新后经工作台重开原任务：主textarea=1，用户反馈、完整v3和接受成果按钮保留；没有点击接受。UI反馈出现多次是历史重复提交留下的记录，不宣称对话去重或排版全部完成。

证据：`rqa28-vd-f01-actual.json`、`rqa28-vd-f01-wire.json`、`rqa28-feedback-grading.md/.json`、`rqa28-d01-case-and-ui.json`。

wire只观测请求，不含两次provider原始响应体。不能断言首轮已正确、是FINALIZE恢复错，也不能断言模型内部因果。可确认的是新规则完整进入请求，而最终交付未落实。

## D01无效诊断与D02未执行

为区分旧多轮历史影响，新建D01 `task-mtpq4x1j-qp5hk`。主线错误地把4773字旧稿及反馈全部放进goal；实际goal/brief.goal均截至2000字，尾部215字反馈不在任何实际请求中。因此**D01专业评分N/A**，不能算忽略已送达反馈，也不能用于排除历史干扰。主线已当面更正此前初读推断，原F01结论不受影响。

D01实际有grep_files一次失败、list_skills两次、load_skill一次成功，不能写零工具；无图片生成、外发或付费操作记录。模型调用与用户测试规则的遵守仍应独立检查，已安装Skill不等于方法始终装配。D01响应不属于有效的同条件修订对照。

准备D02把长正文放到材料字段、短反馈放goal，但新任务启动被auto-review拦截，原因是D01工具调用与测试禁止工具边界冲突。没有换途径绕过；实际任务列表无D02。若继续该真实对照，需要先明确批准隔离测试中允许的模型/只读工具边界，或完成可验证的宿主强制禁工具路径；不得仅靠重复写禁止工具提示后重试。

证据：`rqa28-vd-d01-actual.json`、`rqa28-d01-case-and-ui.json`（保留原拟定输入，不回填）、`rqa28-all-wire.json`（F01两请求+D01五请求）。目标字段2000字静默截断尚未修；不能把主线使用错误字段和平台截断合并成模型能力判断。

## 验证、范围和下一步

- 主线完整 `npm run check` session45697 已exit0；包含backend、lint、renderer、renderer typecheck。没有把观察输出为空当作重新跑过一套测试，也不借上轮计数填本轮。
- 独立定向复跑53/53通过，无新增阻断级P1；明确为fixture工程验证。
- `git diff --check` exit0，仅既有windows-gpu-policy两处CRLF提示。
- 最终GitNexus整树扫描323文件、706符号、172受影响流程，CRITICAL；包含共享历史脏改动，绝不归为本轮两处改动或声称全树已审。未提交、未覆盖无关修改。
- RQA26冻结的16个文件仍原hash；这16个不含本轮修改的两源文件，不以其“无差异”冒充本轮没有代码改动。
- 两批请求观察器均restore，后一个0请求。自有QA最终85任务、20专家ID、无starting/running/revising；D01不增加专家覆盖，仍有4位无基线。未触及日常APPDATA或重启日常实例。

当前源码指纹：runtime `64ebfad4be37e752d77754932e1552887ea1f0f3379b12f97775f99a7b87927d`；model-tool `c80c4958a8a2cf253c84d76809fec2506b70544bf7ed1b7f2e9261f0d31a17f3`；新增测试 `cbe53fe6b003fe9b9400ad0b71141ceb8bff738484a4cb8ea8ad7201f812e00e`。

下一步应优先补足可验证的输入/材料契约与诊断隔离，再取证原始两阶段响应区分修订失败来源；专业方法、实际装配和新场景复验继续。不得继续仅叠加通用提示并把绿色工程测试当作专家质量提升。用户已要求的24专家正常/异常/修订闭环、通用工具与上下文、知识/飞书及完整体验验收仍未完成。

本轮GitNexus Skill约束了公共高风险修改范围，webapp-testing Skill用于真实主输入/刷新重开验证；这些方法没有替代独立专业全文评分。
