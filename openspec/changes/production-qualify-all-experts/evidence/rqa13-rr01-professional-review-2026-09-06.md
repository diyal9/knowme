# RQA13 RR01 修复后独立专业评审

## 结论与取证边界

**新交付按冻结五断言为2/5（40%），专业验收不通过；平台review/verified成立，但不是专业质量证明。**

- task：`task-mtowzdmo-c9tm5`；本次唯一计分run：`expert_task-mtowzdmo-c9tm5_mtozc5xx`。
- 读取 `rqa13-rr01-postfix-live-2026-09-06.json` 的 `session.session.messages`，按role=assistant及完整runId筛选，唯一匹配；没有使用截断的task.resultSummary评分。
- session为`wb-expert-task-mtowzdmo-c9tm5`，与task.execRef一致。对应成果的executionRef绑定同run，artifact.body与assistant.text逐字相等；assistant存储answerHash是正文SHA256的前16位。
- 冻结 `professional-methods-workspace/frozen-inputs.json` 的RR01.input与task.brief.materials中RR01.content相同，同run user.text也完整包含该输入。读完同run user、assistant及trace；历史拒答不混分。
- 本次按skill-creator的`agents/grader.md`及`references/schemas.md`做二元断言与额外声明核对；不因表面出现WHEN/THEN给分，不将“如”的候选值错算成既定事实。不运行模型/API，也不修改包、src、冻结输入/输出或旧12run评分。
- 新grading位于 `../skill-evals/professional-methods-workspace/post-runtime-fix/RR01/mtozc5xx/grading.json`，独立于iteration-1对照。

下文L编号均为对应正文`text.split('\n')`的一基行号，不是JSON文件物理行号。

## 冻结五断言评分

| 项 | 结果 | 全文证据及理由 |
|---|---|---|
| 1 权限冲突、需求/实现证据边界 | PASS | L1“阻塞（需修改）”；L5准确定位§1/§2，用“若按§2实现”说明风险，没有声称已发生漏洞。L7括注§1为测试假设且须先解决冲突。 |
| 2 软删除冲突、候选澄清 | PASS | L10定位§3/§4，L11列待决策选项，L12按决定分支；没有擅定唯一口径。后续验收方法有错，计入第4项，不抹掉正确识别冲突的证据。 |
| 3 并发/超时待定、不强推规则 | FAIL | L16/L21列待决策，但L22把“继续运行”直接转成“提示后1分钟应能下载完整文件”，加入未经选择的完成时限。全篇开头的“不替业务方选定”不能抵消局部硬预期。 |
| 4 可测性及条件前提 | FAIL | L25指出不可测且L27提供计时起止，但完整性仅用行数；L12未组合权限范围；L17同链接不能证明任务去重；L22一分钟成功没有材料支持。出现条件词不足以保证条件正确。 |
| 5 非目标、静态边界、精简结论 | FAIL | 无审批/邮件及动态成功宣称，位置/优先级完整；然而全文741混合单位，超过冻结700上限，故整项不授部分分。 |

## 全部WHEN/THEN及额外规则复核

| 正文段 | 已做到 | 不能直接用于验收的部分 |
|---|---|---|
| L7 权限 | 对普通成员、§1假设、包含他人记录失败说清楚；不属于无条件选择§1。 | 宜明确本项目、经最终删除口径过滤的预期记录集合；没有管理员例是覆盖限制，本题未要求所有角色逐条示例，不单独算硬失败。 |
| L12 删除 | 排除/包含两分支条件化。 | “项目”非软删/总记录数未与角色过滤组合。例：采§1且排除软删，成员自建2条、他人8条均未删；合规导出2条，但该计数例可要求10条。即便按管理员理解，行数相等也不能证明记录正确。 |
| L17 并发 | 保留选择去重/允许重复。 | 同链接不等于同任务：两个任务共享同一文件URL仍可通过“相同…文件链接”的OR判据。允许重复也不蕴含两任务必成功，需说明正常执行前置及失败分支，不能把两次创建和两次成功混为一事。 |
| L22 超时 | 继续/取消表面分支明确。 | 继续运行不蕴含提示后1分钟就绪；合法的长耗时任务会被误判失败。取消分支“不得产生文件”也未区分取消生效、完成竞态及临时文件，不能从未定生命周期直接推导该全称断言。 |
| L27 性能/完整性 | 从点击到文件生成完成的计时边界清楚，T保留待确认；1万条位于举例上下文，不按既定规模误判。 | 原文要求“不丢数据”，仅总数相等不足。反例：应导A、B，实际A、A，计数都为2；字段错值亦不改变计数。应按最终权限、删除、筛选口径得到预期身份集合，再检查每条身份/次数/字段。 |

额外规则的证据分级：

- L21“如24小时”、L26“如1万条…<10秒”属于明确候选例，不是虚假事实；性能例下一行还有待确认阈值。真正失守的是L22的硬性一分钟完成保证。
- L11“可选包含软删除”的配置项属于待决策新增功能候选，不是已获批准方案。它扩大了简单规则裁决的设计选项，应标注范围/成本并经确认；本次不把该建议本身判为编造。
- L5“越权风险”、L15“可能资源浪费/用户困惑”是有条件的风险推论，不是动态复现报告。
- L29“否则无法进入开发阶段”是偏宽的流程结论。可支持的是冲突相关实现与确定性验收未就绪，不足以禁止所有技术预研或条件化测试设计；属于范围过宽，不是本次主要扣分依据。
- 完整正文没有新增审批/邮件，没有声称测试失败/通过；同run toolCalls为空，支持本次未用工具的记录边界，不单靠输出自述判断。

## 前两份观察稿：独立附评，不并入新交付得分

来源 `rqa13-rr01-observed-rounds-2026-09-06.json`；两份均属于旧run `expert_task-mtowzdmo-c9tm5_mtoz5d0m`。这是两次verification pass，不冒称两个MODEL round。文件标记observerRestored=true；两次材料hash均与冻结RR01一致，快照hash相同。唯一门禁错误都针对“评审结论：阻塞（需修改）”，**这不代表正文没有其他专业问题**。

### 观察pass 1

- 冲突及待定项识别正确，权限例有“若维持§1”条件；不能因平台误拦标题就判正文全错。
- L12包含分支仅要求软删除行数`>0`：若预期5条，漏4条仍通过；还额外要求“标记状态”，§4没有规定导出状态字段。排除/包含判据也缺权限交集。
- L27用`CSV行数=数据库count(*)`，同数量漏/重/错值仍通过，且没有限定相同权限/删除/筛选范围；CSV格式是未获选择的实现细节。
- L22“一分钟查询返回进度或结果”**不同于新交付的一分钟必须下载**，允许继续处理中，不把它判成相同的完成SLA错误。仍需说明T的起点、查询能力及保留边界；30秒提示本身来自材料，应保留。
- L16将前端防抖、后端去重与允许重复并列，混淆业务语义和实现手段；前端防抖不能单独证明服务端不重复。L15“数据覆盖风险”为可能性推论，材料未给覆盖机制，支撑较弱，但不是已发生覆盖的虚构。
- 全文689混合单位，**未超700**，尽管raw=1098；不以Markdown原始长度处罚。

### 观察pass 2

- 权限WHEN显式包含他人记录，删除例选择§3条件化；L30补上关键字段逐条比对，比仅比行数更有鉴别力。
- **明确硬伤：性能判据循环设前提。** L29把“耗时≤T秒”放进WHEN，L30 THEN只验数量/字段。耗时>T时WHEN不成立，慢导出反例不会按该例判失败；应让规模/条件留WHEN、实测耗时≤T留THEN。参数待定不能修复条件位置错误。
- L10“选定前不得编写相关自动化用例”为材料未授权的流程禁令；可以先写待决策/条件化测试，不能将尚无最终验收基线等同于不能设计测试。
- L9若采§3只删§4“包括软删除”措辞，仍剩“必须包含所有状态记录”，没有明确软删除例外，回填建议可能保留冲突。
- L41禁止进入开发与测试过宽；正文反而已经给出可条件化设计，不能把这些例子一概视为不允许存在。
- 全文725混合单位，超过700；raw=1168。未新增动态验证成功事实。

## 最小专业改进与可验收反例

不需要扩写成更长方法。最小要求是：**局部THEN继承全部已选前提；未知数值继续占位；验收必须能排除具体错误输出；最后核对全文长度。**

可用于后续未见题rubric的通用反例：

1. 身份集合错误但总量相等，完整性判据是否仍会放行。
2. 两条独立规则都已选择时，是否检查它们的交集，而非各自套全量集合。
3. 异步操作仍进行中时，是否无据承诺某个固定时点完成。
4. 同输出地址但多个执行实体，是否错将结果相同当执行去重。
5. 性能不达标时，WHEN是否仍成立、THEN是否真正能判失败。
6. 明确候选建议是否在总结/示例中未经选择变成硬义务；必须同时检出这种转变，并放过真正保留条件的建议。

以上是后续rubric建议，**不追加到本次冻结五断言分母、不回改旧分数**。

## 计数、hash与复核

计数沿用原对照口径：每个汉字一项，连续拉丁字母/数字词一项（允许词内小数点、逗号、斜线）；标题、表格、尾注全部计入，忽略标点、空白与Markdown控制符。精确匹配表达式：`/\p{Script=Han}|[A-Za-z0-9]+(?:[.,/][A-Za-z0-9]+)*/gu`。

| 文本 | raw字符 | 汉字 | 拉丁/数字词 | 混合合计 | 700上限 |
|---|---:|---:|---:|---:|---|
| 新交付mtozc5xx | 1100 | 699 | 42 | 741 | 超41 |
| 观察pass 1 | 1098 | 636 | 53 | 689 | 未超 |
| 观察pass 2 | 1168 | 667 | 58 | 725 | 超25 |

SHA256：

- frozen-inputs.json：`6c6c22dcb900ce78892bb16e855507d67fae32faca49125ac7eff5df9b21e5db`
- RR01输入正文：`f3ba2dbfc83e96bfc9c403ef9c97daadd12570d550a080b4f09c7e3481691fd2`
- postfix-live capture：`84920d0284ad385197f9b23ce1db8c092cbba9845e25d73f392a436550a94be1`
- 新assistant正文：`b1ad685433d943bd033bf59c4efde7e50e3617b7ed538547986541e27c1f61b2`
- observed-rounds capture：`f7389aabb8c351de3f2c4f0d47ef5416618295e47e096a87f39c581309900491`
- 观察pass 1正文：`bc68c95cb4121a66ef7fb6fa030fb30a778e0e4a801fe2d56432968a180abeba`
- 观察pass 2正文：`5b0568420936adf1915822c84facdcee0774ddb863a594150e713a37b1be3b74`

只读Node核对了目标assistant唯一性、输入一致、成果正文一致、文本计数及上述hash。执行时长/模型token没有同run独立计量证据，未伪造这些指标。新增report和grading之外无写入。
