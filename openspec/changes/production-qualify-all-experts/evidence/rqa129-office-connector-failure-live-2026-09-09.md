# RQA129：办公协作连接器失败闭环

日期：2026-09-09。使用同一隔离 Electron 资格入口，将本地飞书 CLI 夹具切换为授权失败，执行 OP12 `today-priority`。

## 结果

- 任务状态：`needs_input`
- 注意类型：`authorization_required`
- 任务没有生成成果物
- 运行失败数：`0`
- 没有伪造日程、待办或优先级结论
- UI 可操作动作：引导用户打开能力授权

失败信息明确指出 `feishu` 连接器尚未完成用户授权，并列出 `feishu.today_priority` 工具不可用。该结果证明外部连接器失败会停在授权边界，不会被模型夹具或普通文本包装成成功。

## 边界

这是异常处理和拒绝伪执行的运行时证据，不代表真实飞书生产账号授权已完成。
