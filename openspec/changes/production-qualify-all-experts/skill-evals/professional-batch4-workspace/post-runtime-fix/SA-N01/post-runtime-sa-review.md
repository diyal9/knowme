# SA-N01 runtime修复后独立专业复核

结论：冻结断言 **3/5（通过1、2、4；未通过3、5），专业不合格**。这是一份同安装基线的重复输出，非盲、没有同期独立对照，不是Skill A/B或专家认证。按skill-creator grader/schema逐条评分并额外检查全篇事实，未改原文、冻结题、旧评分或源码。

## 证据绑定与运行边界

- task `task-mtp0nv3k-2kfue`；session `wb-expert-task-mtp0nv3k-2kfue`；run `expert_task-mtp0nv3k-2kfue_mtp0nv96`。
- metadata/transcript输入及5断言与冻结SA-N01一致；answer等于capture.text加文件末尾换行。原始输入SHA256见本次grading.json。
- 同run为MODEL `stop`、1轮、0工具、平台DONE/verified；provider用量8851输入+2583输出=11434tokens，运行28.384秒。这些不是专业通过证据，也不证明本次触发过length修复。旧文finishReason未知，不由句尾反推。
- 本次导出无contextAudit，不假定新SOP装配，也不挪用历史run上下文证据。viewer由主线统一生成。

## 专业硬伤及合格边界

1. **密码学兜底不成立。** 推荐乙称客户端漏检后，服务端密钥轮换仍会令旧分片不可解密。反例：断网客户端已持有旧密钥K与旧密文C，服务端换K2不会使本地K/C失效。到期拒绝/删除仍依靠客户端门控；AC1即使因出口检查而拒绝，也不能证明轮换产生了效果。这是推荐依据与验收归因错误，不是措辞偏好。
2. **账号/工作区绑定契约未交付充分（断言3）。** 许可接口有(account,workspace)，数据键却只有ws/id、查询接口没有当前账号绑定说明，版本高水位又写为每账号。不能从物理分片或一句隔离保证补出联合键契约；也不能反向声称已经发生泄漏。逻辑隔离完全可行，C4“混合缓存只能全删/全留”是额外错误。
3. **回退与验收不足（断言5）。** “旧缓存曾做吊销清理”不能覆盖清理后新增撤权；旧读路径回退时必须仍受当前许可与不可回退版本状态约束。AC1未给断网撤权的受控组合，AC5没有冷热缓存、具体硬件/待定参数、查询负载与计时起止。20–40%索引比例及工期估计已标未测，不记成伪造实测，但不能支撑容量或资源结论。

正确部分仍计分：保留5分钟硬约束、明确剩余租期和到期拒绝；在线逐次授权与本地有界许可是两个不同机制；过期在途查询、重启/休眠及乱序拒绝有明确顺序。销售话术“最多5分钟”结合后文剩余期可作上界，未据此判为断网重新续期。甲乙都不能离线8小时，不代表离线能力相同；全文相反说法已列入claims。

三个冻结hard-failure条件未一概标成已发生：当前未声称满足8小时、未指令正常路径重启续期，也没有伪造部署/实测记录。专业不合格依据是上述确定技术错误与未满足的设计要求，而非虚构实际安全事故。

## 样本与比较限制

本次只有 **1题、1份输出、无新增留出countercase运行**。正文UTF-16/codepoint均4316；汉字2639+alnum词块164=2803（heuristic）；去空白含标点/Markdown3927。题目无字数上限，不作长度扣分。

历史评分也是3/5，但失败项不同，不能用相同总分掩盖变化；本次已交付验收章节，仍有上述内容缺陷。既不能据此归因Skill改善，也不能单凭stop/verified或单次通过未来用例就总资格放行。

本次仅新增SA-N01目录下的 `old_skill/grading.json`、`post-runtime-sa-review.md`。两份文档完成后冻结。
