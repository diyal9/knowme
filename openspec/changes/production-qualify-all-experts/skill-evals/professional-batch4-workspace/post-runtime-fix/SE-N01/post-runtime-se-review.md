# SE-N01 运行时修复后独立专业评分

2026-09-06；按 skill-creator grader/schema 全文、二元证据评分。仅本例，同任务 retry；不覆盖旧评分，不是 Skill A/B，不代表软件工程资格通过。

绑定 task `task-mtp0nuq2-oc9ib` / run `expert_task-mtp0nuq2-oc9ib_mtp0w7iu`。metadata.prompt 与 transcript.prompt 相同；answer.md 与 capture.text 除末尾换行完全一致，使用完整最终答复而非 resultSummary。运行记录为 MODEL tool_calls → MODEL length → FINALIZE stop，平台 review/verified 只代表平台门禁结果。

## 冻结五断言：3/5

| 断言 | 结果 | 原文证据与缺口 |
| --- | --- | --- |
| 1 决定性失败时序 | 不通过 | answer.md:7–12双读5、写1/2具体，但没有两个putRequest及成功返回，未明确两请求均成功；“总扣减7”混淆库存实际净减3与业务预留7。73–74覆盖不一致风险，不补足双成功证明。 |
| 2 事务修正与较小改法 | 通过 | 34–55原接口、tx四方法及tenant参数符合M2；73–76解释唯一键后重查无法回滚、requestId锁不能保护不同键库存。不另发明API。未专门讨论库存级进程内锁的跨进程局限，是说明遗漏，不是实现用了错误锁。 |
| 3 幂等/租户/超时 | 通过 | 38–43查原记录、比较sku/qty并返回old.result；64–66及T7区分提交前后超时，坚持相同键重试。没有换键盲重扣或把超时当确定未提交。 |
| 4 参数/不足/异常 | 通过 | 24–35事务前拒绝非法qty；47在写入前拒绝不足；代码无catch吞错，事务承载原子性。提交后超时仍可全量已提交，不属于半提交。 |
| 5 可执行测试设计 | 不通过 | T5只有“同时发起”，无屏障/显式调度；T2仅顺序重放，缺同键并发；T8没有扣库后put失败的精确注入及库存/账本回滚断言；T7没有完整故障夹具初态/注入操作。明确未执行，但未注明M4之外旧覆盖未知。 |

上述不足不能由评分员另写夹具或旧轮probe替原答复补分。本次不执行提案代码、不调用外部模型/API。

## 独立硬失败与额外专业问题

1. **触发冻结无工具硬失败。** 委托明确“不调用工具”，本run执行 `update_plan` 一次且status=ok，callId `call_3ae06a1fbfbc4b50ae850466`，耗时11ms；currentRunMessages亦有对应tool消息。answer.md:152仍称“未调用工具”，是可核对的过程声明错误。虽仅内部计划，不是DB/文件/部署副作用，也不豁免明示禁用工具。不能据此声称代码已被测试。
2. **可串行化不等于物理无穿插。** 第62行把M2加强为“不可被其他进程穿插”；第146行又承认全租户锁未知。代码依赖可串行化即可成立，应区分结果保证与实现机制。
3. **条件警示不是当前实现的反例。** 第16–17行全局requestId警示有“若”且承认当前传tenant，不能把它判成凭空声称串账已发生；但第3行“两个最小反例”的归类不成立。第二个决定性反例更应给出扣库后账本失败时序。
4. 第147行将tx签名称为“假设”，而M2已给“相同四方法”；真实适配检查可列后续，但不是本题信息不足。未发现当前修正代码违反给定签名。T7对两种超时的描述是受控场景推演，不认定其声称客户端可区分两种超时。

正文3842字符（文件含换行3843）；去代码块/空白/部分Markdown标记的正文代理计数2038。无冻结长度上限，不因raw Markdown长扣分。问题是证明及测试步骤不足，不是短答复本身；也不需要靠扩写Skill或增加无关架构来解决。

## 可验收的最小改进

补全原错误执行的两条账本插入与成功返回，明确“业务预留7，库存落2”；再给扣库成功、账本失败的状态反例。测试补明确调度点、同键并发与历史result、写账本失败回滚、提交成功后丢ACK再原键重放，并断言库存和账本两侧。无工具任务直接静态作答，过程声明必须与实际记录一致。

完整逐项证据、task/run与三个冻结输入SHA256见 `old_skill/grading.json`。本次仅新增该评分和本短评，不修改原文、源码、包或旧评分。
