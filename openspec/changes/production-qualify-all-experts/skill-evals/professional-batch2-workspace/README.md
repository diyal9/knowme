# 三专家真实基线

会议纪要、事实核查、数据报告，源包2.0.0，隔离Electron+实际Qwen3.8Flash。输入在执行前冻结；运行没有模型夹具。基线通过API确认正式执行，不证明规划/选项UI。每题一次，不能代表生产资格或稳定性。会议例因平台误加外部工具要求而在模型调用前失败，不能评价未发生的专业答复。

用户显式请求单一answer，brief.deliverables使用primary。三包声明output-1/output-2不匹配primary，未强制requiredSkills；日志L0摘要在、非方法正文完整装配。因此这些是当前产品的用户单一交付入口基线，不是完整Skill调用对照；后续必须明确版本/装配差异。没有成本/耗时数据时不填0。

