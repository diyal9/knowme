# QA Plan — dedupe-workflow-run-back-chrome

## Smoke Scope

1. 从货架启动任一工作流 → 进入运行面：只见 `#wbRunBack`，全局头不出现空条 / 孤立 chevron。
2. 点「返回」→ 回到货架，进行中运行不无故中断。
3. 打开专家任务房 → 顶栏返回仍可用，可退出任务房。
4. 进入编排页 → 顶栏返回仍可用，可回货架。
