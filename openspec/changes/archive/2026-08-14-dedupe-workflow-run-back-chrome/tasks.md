## 1. 运行面顶栏去重

- [x] 1.1 `syncHeadActionButton`：工作流运行面不再显示 `#wbReload`；保留 studio / 专家任务房
- [x] 1.2 工作流运行面隐藏 `#wbHead`（或 `.wb-head`）；离开 run / 进入专家任务房时恢复
- [x] 1.3 更新 HTML 注释与必要时 `id="wbHead"`；补契约测试断言运行壳返回唯一

## 2. 自测

- [x] 2.1 跑 `npm test` / `npm run lint`；写 `evidence/dev-self-test.md`
