# QA 计划

## Smoke Scope

- [x] 打开 Agent 对话：输入框上方不再出现「本轮带上」或记忆勾选芯片
- [x] 输入草稿 / 空态：composer 旁仍无工作提示条
- [x] 发送消息：不因缺少勾选条而报错
- [x] 设置协作偏好或已确认习惯时，对话仍可静默带上个性化上下文（无 UI 开关）
- [x] 全量单测与 lint 通过

## 回归 Scope

| 项目 | 结果 |
|---|---|
| 快捷操作 / suggestion 条不受影响 | 通过 |
| `buildWorkHints` API 仍可单测 | 通过（后端保留） |
| `buildUserPrompt` 默认仍注入协作偏好 | 通过 |

## 反模式检查

| 反模式 | 结果 |
|---|---|
| 留下半残 UI（空壳勾选） | 已清：相关 DOM/状态/刷新全删 |
| 切断有用的静默记忆 | 未切断：主进程个性化保留 |
| 未更新测试仍断言旧 UI | 已改：断言提示条不存在 |
