# Tasks: note-close-resume

- [x] 1. main：`lastClosedNoteId` + `resumeAfterNoteHide`；接入 hide / close→hide / 右键关闭；托盘「继续编辑」
- [x] 2. preload + list：`list-highlight` 高亮滚动；关窗文案
- [x] 3. 冒烟单测（preload / list 标记）+ `npm test` / lint + 证据
