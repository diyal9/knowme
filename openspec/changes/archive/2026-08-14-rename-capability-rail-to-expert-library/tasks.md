## 1. OpenSpec

- [x] 1.1 完成 proposal / design / specs / tasks / qa-plan
- [x] 1.2 明确改/不改边界（模块名 vs 条目级「添加能力」）

## 2. UI 文案

- [x] 2.1 `workspace.html`：rail title / aria-label / rail-label →「专家库」；宿主 brand / aria →「专家库」；工作台静态 CTA
- [x] 2.2 `capability-hub.html`：document title 与顶栏品牌 →「专家库」
- [x] 2.3 `workspace.js`：drawer / iframe 标题 →「专家库」
- [x] 2.4 `workbench.js` / `workspace-agent.js` / `main.js` / `settings.html`：导航与 toast 中的能力界面/中心/Hub →「专家库」

## 3. 测试与证据

- [x] 3.1 更新 `tests/workspace-capability-rail.test.js`；新增命名边界测试；修正依赖旧文案的断言
- [x] 3.2 `npm test`、`npm run lint`、写入 `evidence/dev-self-test.md`
