## 1. 分层状态与渲染

- [x] 1.1 在 `workspace.js` 增加 `fileCenterLayer`（hub/tree）及进入/返回辅助函数
- [x] 1.2 重写 `renderSourceTree`：hub 渲染源管理分区；tree 渲染紧凑切换条 + 文件树
- [x] 1.3 复用 `#btnProjectBack`：树层返回源中心；hub 隐藏返回

## 2. 交互接线

- [x] 2.1 选源成功后进入树层；「切换」进入源中心
- [x] 2.2 hub 搜索过滤源名与 AI 产物；tree 搜索保持文件过滤
- [x] 2.3 补充切换条样式（`workspace.html`），去掉树层冗长说明卡片

## 4. 顶栏精简

- [x] 4.1 「打开」「切换」移入侧栏顶栏；树层隐藏添加/设置/折叠/更多
- [x] 4.2 身份条仅保留源名与类型，不再内嵌按钮
