## 1. DOM / 动作

- [x] 1.1 审阅 foot 增加「刷新」按钮；`renderDaemonRunner` 去掉 refresh/back，空底栏隐藏
- [x] 1.2 CSS：foot 并排自适应宽度；空 `#wbRunnerActions` 不占位

## 2. 自测

- [x] 2.1 更新契约测试；`npm test` / `npm run lint`；写 `evidence/dev-self-test.md`
