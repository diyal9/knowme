## 1. 选择策略

- [x] 1.1 底栏「全选」旁增加策略按钮：已完成 / 超过 1 个月 / 超过 3 个月 / 清空
- [x] 1.2 实现策略勾选逻辑（替换勾选；年龄看 updatedAt）并同步删除按钮态

## 2. 任务卡片

- [x] 2.1 卡片渲染专家头像 + 名称（availableExperts + agentAvatarMark）
- [x] 2.2 进度详情用 `<details>` toggle，默认收起；点击不误触勾选

## 3. 样式与自测

- [x] 3.1 补充 `workbench-layout.css` 策略组与卡片布局
- [x] 3.2 更新 `workbench-templates.test.js` 断言；`npm test` + `npm run lint`
