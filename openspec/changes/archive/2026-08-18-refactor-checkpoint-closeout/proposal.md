## Why

本轮可运行，但不能以「重构完成」提交并进入下一轮。工程门禁约 90%，界面重构约 80%，长 Markdown 主线程卸载刚补上但仍缺真机，产品面多项仍标「薄」，OpenSpec 仍有未完成任务。需要一次**明确收口迭代**：记账、分流、真机签字，而不是再开新功能。

## What Changes

- 把当前树定为 **checkpoint**，禁止对外宣称重构完成
- 流程：从未开工的大 change 降为 backlog，不在本迭代实现；已完成且有证据的才归档
- 产品：`surfaces.md` 仍为「薄」的主工作台 / 设置 / 文件中心 / 任务房 / 管线首页做制作人真机走查并签字或列缺口
- 性能：确认长文 Worker 路径后，真机验证首屏不卡；不把「架构搭好」当成完成
- **不**清掉 restore-* 的诚实缺口，**不**新开下一轮功能

## 目标用户

制作人与开发：知道现在能跑什么、差什么、下一刀砍哪。

## 验收标准

- 文档与 health / `openspec list` / `git ls-files`（非 archive）数字一致：活跃 change = 0
- 未交付 epic 与 restore 缺口只在 BACKLOG，不占活跃目录
- 薄表面有真机结论（通过或缺项列表），无无证据改「有」
- 本文件关闭的是**检查点**，不是整包产品 1:1

## 非目标

- 不实现全部未完成任务
- 不把 restore-game-studio-ui-parity 标完成
- 不加新表面
