# note-minimize-to-tray（delta）

## MODIFIED Requirements

独立便签最小化到托盘不再作为现行需求。托盘只服务工作台主窗。

#### Scenario: Tray does not manage note windows

- **WHEN** 用户使用托盘菜单
- **THEN** 没有「显示便签 / 关闭便签」类条目依赖 `noteWins`
