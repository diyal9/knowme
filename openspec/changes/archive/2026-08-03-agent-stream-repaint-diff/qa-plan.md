# QA Plan: agent-stream-repaint-diff

## Smoke Scope（必填）

- [ ] 长回答流式输出时，「执行进度」呼吸球动画连续不重启，耗时每秒跳字但整卡不闪
- [ ] 流式过程中展开某工具步骤详情，下一事件到来后详情保持展开
- [ ] 流式过程中折叠「执行过程」卡片，后续事件不强制展开
- [ ] 首个 token 到达时历史消息稳定，无整页闪一下
- [ ] 含表格 / 代码块 / 飞书链接卡片的回答最终排版正确
- [ ] 含 ` ```suggestion` 的回答在流结束后建议卡片正常出现且可点击
- [ ] 中途点「停止生成」，时间线定格、无残留 pending 行

## 反模式挑战

- 快速连发多轮，看时间线是否串行错乱
- 在流式中滚动到历史位置，检查是否被强制拉回底部（near-bottom 阈值 96px 之外不应跟随）
- 窗口缩到很窄再流式输出，检查表格与卡片不抖

## 回归

- `npm test`（737 项）、`npm run lint`
- Agent 会话切换 / 新建会话 / 历史加载
