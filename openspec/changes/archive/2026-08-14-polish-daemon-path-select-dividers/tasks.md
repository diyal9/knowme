## 1. 交付路径自定义下拉

- [x] 1.1 将 `wbDaemonComposePath` 改为自定义 combobox（触发器 + listbox + 同步隐藏 select）
- [x] 1.2 CSS：淡色行间分割线、选中/悬停/禁用/focus，对齐 `--daemon-*` 令牌
- [x] 1.3 绑定：点击选项同步 value 并触发既有 change 流；外点/Escape 关闭

## 2. 门禁

- [x] 2.1 `npm test` / `npm run lint`
- [x] 2.2 写 `evidence/dev-self-test.md`
