## Smoke Scope

- [ ] 工作台顶栏无「进行中」按钮
- [ ] 任务首页仍可打开运行中 / 最近任务
- [ ] 管线服务「进行」筛选仍可用
- [ ] 工作流运行中返回货架不中断；无顶栏进行中也能从任务/管线恢复

## 反模式清单

- [ ] 顶栏仍残留隐藏的进行中控件或空白占位
- [ ] 引导文案仍写「货架顶部进行中入口」
- [ ] 删除后控制台报错（空引用 `wbRunningToggle` 等）

## 通过标准

静态契约测试通过；本地 `npm test` / `npm run lint` 通过；上述冒烟无阻塞问题。
