# Tasks: rename-workbench-tab-copy

## 1. Copy in HTML / JS

- [x] 1.1 `workspace.html`：顶栏「专家协作」；专家协作页 / 工作流页文案按 glossary 替换；去掉「货架」
- [x] 1.2 `workbench.js`：用户可见字符串（空态、返回、删除确认、composer、管理弹窗、卡片「开始运行」等）同步
- [x] 1.3 轻量注释/aria 与 Tab 名对齐（不改 id）

## 2. Tests & evidence

- [x] 2.1 更新 `tests/workbench-templates.test.js` 相关断言
- [x] 2.2 `npm test` + `npm run lint`；写 `evidence/dev-self-test.md`
