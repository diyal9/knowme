## 1. Store 可选摘要字段

- [x] 1.1 在 `workbench-task-store` 归一化可选 `resultSummary`（短文本），旧数据无字段仍可读
- [x] 1.2 若任务更新路径已有结果文案可传入，则写入 `resultSummary`（无则跳过，不强制）

## 2. 卡片渲染与摘要

- [x] 2.1 将 `renderTaskRecentRow` 改为三列卡片结构（标题、状态、专家/时间、摘要、定时）
- [x] 2.2 实现摘要优先级：`resultSummary` → `goal` → 状态短句；摘要行数截断

## 3. 布局与高度

- [x] 3.1 CSS：`.wb-task-recent-list` 改为三列 Grid，窄屏降列
- [x] 3.2 收起态控制高度（预览 3 = 一行），展开态列表容器内滚动，避免收起时页面滚动

## 4. 自测与证据

- [x] 4.1 `npm test` 与 `npm run lint` 通过
- [x] 4.2 重启应用目视：三列、摘要、收起/展开与定时/打开任务
- [x] 4.3 写入 `qa-plan.md` 与 `evidence/dev-self-test.md`

## 5. 快捷专家一排折叠

- [x] 5.1 快捷专家网格改为三列（窄屏降列），预览一排
- [x] 5.2 超出时「更多 / 收起」；展开后可操作全部专家，列表区内滚动
- [x] 5.3 更新测试与自测证据，重启验证
