# Acceptance — retire-sticky-icons-naming

- [x] `src/` 无 StickyIcons / sticky-icons / useStickyIcons 残留
- [x] 全局 `window.KnowMeIcons` 与 TS 声明一致
- [x] Icon / TreeIcon / 各 Surface hook 引用新模块
- [x] `npm test`、`npm run lint` 通过
- [x] OpenSpec tasks 全勾选
