## 1. 工作模式模型与主进程边界

- [x] 1.1 实现版本化 `workbench-mode-store`，包含三类内置模式、原子持久化、规范化、默认降级和绑定数量限制
- [x] 1.2 为模式选择、Expert 绑定/移除和完整 DTO 增加单元测试，覆盖重复绑定、非法输入、损坏状态与内置角色保护
- [x] 1.3 在主进程聚合 Capability Expert 与 Daemon 状态，注册 mode list/select/bind/unbind IPC，并扩展 `workbench-load`
- [x] 1.4 在 preload 暴露最小工作模式 API，补充 IPC/边界静态测试

## 2. 工作台信息架构与工作模式体验

- [x] 2.1 调整工作台 DOM 为“总览 / 团队 / 工作流”，增加工作模式切换器和独立团队页面，同时保留现有任务工作间与自动化页面
- [x] 2.2 在 `workbench.js` 建立当前模式投影、切换与刷新逻辑；软件研发继续消费现有 Daemon workflow，其他模式诚实空态
- [x] 2.3 重构总览为当前模式、专业能力、继续工作、常用工作流、团队摘要与今日待办，移除全局 Daemon 专家墙心智
- [x] 2.4 实现团队页：区分专业能力内置角色与用户添加 Agent，支持添加入口、状态解释和安全移除
- [x] 2.5 完成暖灰编辑式视觉、工作模式差异化 token、键盘焦点与窄窗口响应式样式

## 3. Capability Hub 组队闭环

- [x] 3.1 为 Expert Drawer 增加“添加到工作台”双动作，复用安装/启用准备逻辑并处理进行中与已绑定状态
- [x] 3.2 实现 Hub 请求/结果消息桥，宿主校验 frame 与 Expert ID 后调用 mode bind API
- [x] 3.3 绑定成功后刷新工作台团队且 Hub 保持可继续选人，失败时保留能力状态并显示可重试错误
- [x] 3.4 扩展 Hub 与 Workspace 测试，覆盖伪造消息、重复绑定、安装后绑定失败和既有“开始对话”回归

## 4. 验证与证据

- [x] 4.1 增加工作模式渲染与工作流过滤回归测试，确认非研发模式不继承编码目录、研发模式启动路径不变
- [x] 4.2 运行 OpenSpec 严格校验、`npm test` 与 `npm run lint`，修复全部硬门禁问题
- [x] 4.3 重启 Electron 手动验证三模式切换、团队组建、Hub 添加、Daemon 在线/离线和重启恢复，无控制台错误
- [x] 4.4 完成 `qa-plan.md`、`acceptance.md` 与 `evidence/dev-self-test.md`，记录截图和已知后续运行时范围
