## 1. CSS

- [x] 1.1 压矮 `.wb-shelf-chip`：去掉偏高 `min-height`，垂直 padding 对齐简要流程步骤条
- [x] 1.2 同步 `.wb-workflow-manage-chip` 与货架条同度量；条间距改为更紧凑 gap

## 2. 自测

- [x] 2.1 肉眼确认首页与管理页 IO 条变矮且文案仍可读
- [x] 2.2 `npm test` 与 `npm run lint` 通过
