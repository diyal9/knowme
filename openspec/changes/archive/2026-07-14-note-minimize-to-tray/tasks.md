# Tasks: note-minimize-to-tray

- [x] 1. main：`minimizeNoteToTray` + IPC；`restoreAppWindows` 优先 `lastClosedNoteId`
- [x] 2. preload + note.html：顶栏改为最小化图标/文案；设置页删除文案修正
- [x] 3. 冒烟单测 + `npm test` / lint + 证据
