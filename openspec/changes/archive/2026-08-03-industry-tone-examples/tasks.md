# Tasks: industry-tone-examples

- [x] 1. 新增 `src/lib/industry-profile.js`（行业枚举、口吻、goalExamples、空态正文）
- [x] 2. `settings-secure` 增加 `industry` 默认值与规范化；`settings.html` 增加下拉与摘要
- [x] 3. `product-memory` / `assistant-prompt-router` / `main` 注入行业 profile 与口吻提示
- [x] 4. 更新今日优先级空态：shortcut prompt 规则 + `assistantBodyHtml` 确定性示例
- [x] 5. 补充单测与 settings/workspace 冒烟断言；更新与「禁止举例」冲突的旧断言
- [x] 6. 执行 `npm test`、`npm run lint`，写 `evidence/dev-self-test.md`
