# 制作人体验验收: simplify-explainable-work-hints

## 核心路径

- [x] 默认态：推荐项为一眼可勾选的简短选项，不堆「依据」正文
- [x] 悬停 / 聚焦：同时看到「具体内容」与「为什么推荐」
- [x] 勾选后：内容进入输入框，可改再发，不自动发送
- [x] 可隐藏整组提示；有输入或运行中时不打扰

## 体验标准

- [x] 低打扰：默认信息密度低于改版前两行常驻卡片
- [x] 可控：采用动作是明确勾选，而非误触大按钮
- [x] 可解释：需要时才展开原因，建立对记忆推荐的信任
- [x] 视觉与现有 Agent composer 暖灰风格一致

## 验收依据

- 开发自测：`evidence/dev-self-test.md`（test 739 / lint PASS；默认详情 `display:none`，聚焦后 `display:grid`）
- Spec：`specs/agent-chat-ux/spec.md` 四场景覆盖默认 / 悬停 / 聚焦 / 勾选
- 硬门禁：`npm test`、`npm run lint` 已在开发自测记录为 PASS
- 静态预览冒烟：基于实际 `workspace-agent.js` 样式注入核对（Electron 壳内同逻辑）

## 验收结论

- [x] 通过 / [ ] 不通过
- 验收人：制作人
- 日期：2026-08-03
- 备注：实机是否出现推荐取决于本机是否有已确认协作偏好 / 工作记忆；交互契约已由代码与静态冒烟锁定。
