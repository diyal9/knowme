## Smoke Scope

- 工作台 → 任务 Tab → 「你的任务」
- 任务数 ≤3：无「更多」，全部可见
- 任务数 >3：默认 3 条 +「更多」；点开展开；收起还原
- 展开后列表可滚动；页面 `wb-body` 仍可滚动
- 空态文案与点行打开任务行为不变

## Anti-patterns

- 默认就展开很长列表把快捷任务顶出首屏
- 「更多」在 ≤3 条时仍出现
- 展开后无法滚动到底部任务
