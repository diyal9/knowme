# Design
路由器根据正式交付意图、Agent 数量以及 Human/Action/Condition/Gate/固定交接判断：自由共创留在主题；单 Agent 生成委托单；多 Agent 或显式控制生成可打开确认的工作流草稿。交接 envelope 只包含用户选择的材料和约束，不读取完整会话、长期记忆或凭据。

我的 KnowMe 不进入 graph。结果只提供需要确认的知识/Profile/Agent/流程改进操作，不自动污染组织资产。
