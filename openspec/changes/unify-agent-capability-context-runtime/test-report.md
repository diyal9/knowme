# 测试报告

日期：2026-09-06。范围：Agent 能力聚合与渐进上下文专项；保留工作区原有变更。

## 已取得的验证结果

| 检查 | 结果 |
|---|---|
| `npm run check` 最后稳定补丁复验 | 退出码 0；后端 3270 总计，3219 通过 / 51 跳过 / 0 失败，Renderer 597 通过 / 85 文件，lint、renderer types 通过 |
| `npm run typecheck:lib` | 退出码 0，无新增 ts-nocheck |
| 专项 harness gate | 退出码 0；test、lint、Renderer test/types、lib types 全部通过，soft 为空 |
| 冷 MCP 发现与激活 | 主线程 22/22 通过；500 冷连接器、单连接器 500 工具、动态 schema、撤权和 requiredTools |
| MCP 分页、旧连接器与旧快照兼容 | 主线程 32/32 通过；stdio/HTTP/SSE 分页错误不发布部分成功 |
| 原生知识、缓存、失败分类及邻接回归 | 70/70 通过 |
| Skill 旧会话实时权限与显式 IPC | 16/16 通过 |
| Scope / 子任务 / 飞书实时撤销 | 70/70 通过；邻接知识与能力集成 54/54 |
| 普通工具 registry → IPC → 任务存储 → 专家恢复 | 5/5 通过；批准、过期、缺失回执、结果不确定均有覆盖 |
| `git diff --check` | 退出码 0；未提交代码 |

所有模拟模型集成必须在真实提供的 schema 集合内调用工具，发现/激活由测试模型使用公开入口完成；没有替模型预加载尾部工具来制造成功。

## 最后复验

全量通过后新增的“连续审批恢复链”用例首跑暴露了第二次审批未执行的问题（36/37 通过）。该用例保持失败断言并交回执行治理模块修复，最终状态以本节补记为准；不能只引用上一轮全量绿灯跳过它。

该问题随后修复：主线程恢复集成 25/25 通过（包括 A→B 连续批准、冷进程过期和目标预览），独立 reviewer 已复测并关闭。之后全量暴露的 FINALIZE 预算产物保留问题已修复，相关 32/32 通过；必要 Skill/契约的预算不足仍然阻塞。

冷 MCP 改造后的首次全量有 3 项失败（3181 通过 / 51 跳过）：两条旧 eager 连接器断言和一条旧快照诊断字段判空。已通过公开加载入口保留实际执行/关闭断言，修复空值兼容，并补齐三种 MCP 传输的服务端分页。最后冻结源码再跑 `npm run check` 和专项 gate，均退出 0，结果见上表。51 项跳过不能视为通过，真实供应商边界见下节。

本地复验日志：临时目录 `knowme-capability-check-verified.log` 与 `knowme-capability-gate-verified.log`；不将整份运行日志、用户资料或密钥提交仓库。报告中的可重复测试入口见 evidence/README.md。

## 离线性能对照

相同合成任务“搜索视觉设计文档”，10 次预热、50 次测量；并发测试环境下测量，非真实供应商延迟或质量评分。

| 目录规模 | 可发现 | 选入定义 | 全量 / 选入 schema 估算 token | 降幅 | 本地选择 p95 |
|---|---|---|---|---|---|
| 100 | 100 | 2 | 7522 / 232 | 96.92% | 0.372 ms |
| 500 | 500 | 2 | 37822 / 232 | 99.39% | 1.122 ms |

复验：`node -r ./scripts/register-ts.js scripts/agent-capability-context-benchmark.js`。全部目录可分页发现、相关尾部工具被选入、没有用无关工具填满 8 项。这里仅比较工具 schema，不宣称整个任务总 token 同比例下降。

## 未执行的真实验证

- 真实模型 canary 返回 `credential_or_model_missing`，是测试环境未配置，不代表产品本机一定没有模型配置。
- 盘古、Photoshop、飞书的真实网络闭环和人工窗口体验未在本轮执行；不得以严格模拟通过替代。
- 用户已澄清知识库指 KnowMe 原生 Brain/Wiki/RAG，外部来源可后续配置，不要求飞书 Wiki 链接。Brain 使用真实本地临时数据验证；Wiki/RAG 外部调用使用受限替身验证范围和协议，不冒充线上资料读取。
- 产品经理、视觉设计师、生图专家的供应商级质量、真实成功率和端到端延迟仍须使用已授权测试资源测量。

## 已知维护性提示

lint 有三个 1200 行 advisory：expert-task-runtime.ts、ExpertTaskRoom.tsx 及其测试。未触发硬性门禁；新增职责已优先拆到独立 scope、activation、round-context、approval、recovery、knowledge helper，不能把 advisory 描述成没有任何维护性成本。

GitNexus 对整个脏工作区报告 CRITICAL（末轮检查 320 个变更文件，695–699 个符号，172 条关联流程；统计随补丁变化）。该范围包含既有修改；单符号 impact 多处为 UNKNOWN/部分图，不是安全证明。结合源码调用点、独立 review 和执行测试补足，未进行提交或改写既有提交历史。
