## MODIFIED Requirements

### Requirement: Unified capability entry uses in-page tabs

左侧 rail MUST 提供“Agent 中心”入口。点击后 MUST 打开统一 Agent 中心；中心 MUST 在同一页面内提供“Agent”“Skill”“MCP 连接器”三个顶部 Tab，并允许用户不离开页面地切换资源类型。

#### Scenario: Open Agent Center

- **WHEN** 用户点击 rail“Agent 中心”
- **THEN** 全屏 Agent 中心打开且“Agent”Tab 为激活态
- **AND** 组织 Agent 的主操作语义为查看与发起任务，不与“我的 KnowMe”人格混同
