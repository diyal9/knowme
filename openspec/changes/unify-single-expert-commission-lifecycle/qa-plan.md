# QA

Smoke Scope：新委托默认自动完成；显式选择审阅；多项交付；证据不足；失败、取消、重试；冷启动读取；结束后讨论和后续委托；任务首页和协作页状态一致。

验证：真实任务存储集成测试 + React 页面交互测试 + npm run check。所有模型/连接器调用使用隔离测试回执，不向外部系统写入。

- [x] Smoke Scope 已由运行器集成测试和页面交互测试覆盖；完整门禁通过。
