# 制作人体验自验

- 新委托默认自动完成，平台在全部必需交付和执行条件满足后结束。
- 审阅选择在开始前明确展示；勾选后进入等待，用户接受后结束。
- 自动完成不出现接受/退回入口；结束后仍能追问，并可显式创建后续委托。
- 协作页和任务卡使用同一生命周期投影，结束结果区分完成、未完成、取消。
- 旧验收记录保留；本次不替用户验收历史成果。

验收依据：真实存储与运行器集成测试、React 交互测试和完整检查。KnowMe 开发实例已重启，Vite 和 Electron 主进程启动成功、窗口正常显示。没有调用真实模型执行业务委托，也没有发送消息或修改飞书数据。
